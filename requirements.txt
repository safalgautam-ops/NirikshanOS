# Web framework (WSGI) and production server
Flask>=3.1,<4
gunicorn>=23,<27

# Existing asynchronous data/service layer. Flask routes are dispatched to the
# application's persistent asyncio runtime (app/core/async_runtime.py).
asyncmy

# Required by asyncmy for MySQL 8's default caching_sha2_password auth
cryptography

# Redis client (sessions, pub/sub, rate limiting)
redis

# Password hashing (Argon2id)
argon2-cffi

# Async HTTP client - OAuth token exchange, Resend email API
httpx

# TOTP (time-based one-time passwords for 2FA / authenticator apps)
pyotp

# QR code image generation (for TOTP setup page)
qrcode[pil]
Pillow

# WebAuthn / Passkeys (server-side challenge + verification)
webauthn

# Async S3 client (MinIO) - object storage for avatars, org logos,
# documents, and case evidence (see app/core/object_storage.py)
aioboto3

# YAML parsing for module execution specs
PyYAML

# Testing (tests/ - see tests/README.md). pytest-asyncio lets async test
# functions be written directly; Playwright drives a real browser for the
# end-to-end tier (needs a one-time `playwright install chromium` after pip
# install - not part of the pip package itself).
pytest
pytest-asyncio
playwright
