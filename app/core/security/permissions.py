"""RBAC permission decorator.

Planned for Week 2: a permission_required(permission_name) decorator
that checks the current session's roles/permissions (via
app/features/rbac) before allowing a route to run.
"""
